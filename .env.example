# OpenRouter (OpenAI-compatible). Only needed for record/live modes.
OPENROUTER_API_KEY=

# Fixer and judge MUST be different model families to reduce correlated bias.
FIXER_MODEL=
JUDGE_MODEL=

# replay (default, offline, deterministic) | record (save live calls) | live (call, no save)
A11YFORGE_MODE=replay
