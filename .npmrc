save-exact=true
# typescript-eslint's peer range doesn't list our pinned TypeScript; the mismatch is
# cosmetic (tsc + tests pass). Allow `npm ci` to install the exact committed lockfile on
# clean machines (and in Docker) instead of failing on ERESOLVE.
legacy-peer-deps=true
